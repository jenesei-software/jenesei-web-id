import styled from 'styled-components'

export const AuthorizationBackgroundWrapper = styled.div`
  width: 100%;
  height: inherit;

  & .background {
    height: inherit;
    width: 100%;
    object-fit: cover;
  }
`
