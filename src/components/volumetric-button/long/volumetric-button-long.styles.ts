import styled from "styled-components";

export const VolumetricButtonLongWrapper = styled.div`
  @media (max-width: ${(props) => props.theme.size.tablet}) {
  }

  @media (max-width: ${(props) => props.theme.size.mobile}) {
  }
`;
