import { UIInput } from '@styles/components'
import { PatternFormat } from 'react-number-format'
import styled from 'styled-components'

export const StyledUIInputCode = styled(PatternFormat)`
  ${UIInput};
`
